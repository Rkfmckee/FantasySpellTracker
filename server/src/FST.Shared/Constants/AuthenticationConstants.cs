namespace FST.Shared.Constants;

public static class AuthenticationConstants
{
    public const string KeycloakHttpClient = nameof(KeycloakHttpClient);
    public const string AccessCookie = nameof(AccessCookie);
    public const string RefreshCookie = nameof(RefreshCookie);
}
